from behave import *
import requests
response = None

@when(u'we request a list of projects')
def step_impl(context):
    raise NotImplementedError(u'STEP: When we request a list of projects')


@then(u'we get the list of projects we defined back')
def step_impl(context):
    raise NotImplementedError(u'STEP: Then we get the list of projects we defined back')